/**
 * Custom field mappings and WHERE conditions for creative_perf_event table
 */

const CREATIVE_PERF_EVENT_MAPPINGS = {
  fields: {
    // Add custom SQL expressions as needed
  },

  conditions: {
    // Add table-specific WHERE templates as needed
  }
};
