# Refactor

## Code Arch refactor

- As the code base is getting larger and larger, the JS code and those assets should follow the best practice of how JS browser extension development convention

## Code Style refactor

- Current, there is many comments inside the code. It's prefer to write code as it's self-explainable then using comment to explain, so we'll also update this to make the code cleaner.
- Follow the clean code pattern to make the code readable and short like early return to avoid nested or long if else logic chain or Guard principle to handle different state of errors
